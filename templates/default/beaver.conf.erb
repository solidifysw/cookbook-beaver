# This file is managed by Chef for <%= node['fqdn'] %>
# Do NOT modify this file directly.
#
[beaver]
<% @beaver.each do |key, value| %>
<%= "#{key}: #{value}" %>
<% end %>

<% @files.each do |file| %>
[<%= file["path"] %>]
  <% file.each do |key, value| %>
<%= "#{key}: #{value}" unless key == "path" %>
  <% end %>
<% end %>
